# Trimmed dependency set for running Rantella with game_id: rdr2
# (faster-whisper STT + llama-cpp-python LLM + piper_binary TTS + chromadb memory).
#
# The full requirements.txt additionally installs every optional TTS engine
# (XTTS, F5-TTS, StyleTTS2, coqui, moshi, omnivoice, ...) and vision/OCR
# extras, several of which are local editable installs that don't exist in a
# sparse checkout. Pantella's module loaders import optional engines inside
# try/except, so leaving them uninstalled only logs a warning at startup.
#
# NOTE: llama-cpp-python comes from JamePeng's prebuilt CUDA 12.8 wheel for
# Python 3.10/Windows (no CUDA Toolkit or compiler needed). On another
# Python version or OS, pick the matching wheel from
# https://github.com/JamePeng/llama-cpp-python/releases — or build from
# source (git+https://github.com/JamePeng/llama-cpp-python.git) with
# $env:CMAKE_ARGS = "-DGGML_CUDA=on" set (needs VS Build Tools + CUDA
# Toolkit + CMake, and git config --global core.longpaths true on Windows).

setuptools==81.0.0

# --- Core ---
numpy==1.26.4
scipy==1.15.3
pandas==2.3.3
requests==2.33.1
httpx==0.27.0
aiohttp==3.10.5
tqdm==4.67.3
packaging==26.2
typing-extensions==4.15.0
pydantic==2.11.7
filelock==3.12.4
json5==0.14.0
jinja-try-catch==0.1.1
websockets==12.0
emoji==2.8.0
text_unidecode==1.3
scikit-learn==1.7.2
safetensors==0.7.0
huggingface_hub==1.3.0
hf-xet==1.2.0

# --- UI / app server ---
gradio==6.15.2
gradio-client==2.5.0
fastapi>=0.112.4

# --- LLM ---
llama-cpp-python @ https://github.com/JamePeng/llama-cpp-python/releases/download/v0.3.43-cu128-win-20260717/llama_cpp_python-0.3.43+cu128-cp310-cp310-win_amd64.whl
openai==1.6.1
tiktoken==0.4.0
transformers==5.3.0
tokenizers==0.22.2
accelerate==1.13.0

# --- STT (faster-whisper + mic capture) ---
git+https://github.com/Pathos14489/faster-whisper.git
# faster-whisper pulls pyannote-audio -> speechbrain; 1.1.0's lazy k2 module
# raises ImportError when introspected (upstream pins 1.0.0 too)
speechbrain==1.0.0
ctranslate2==4.7.1
av==16.1.0
onnxruntime==1.23.2
speechrecognition==3.10.1
pyaudio==0.2.14
soundfile==0.13.1

# --- Memory (ChromaDB + torchmoji emotion tagging) ---
chromadb==0.6.0
posthog==3.5.0

# --- Audio playback / processing ---
pygame==2.6.0
librosa==0.11.0
ffmpeg-python

# --- Torch (CUDA 12.8 wheels) ---
--find-links https://download.pytorch.org/whl/cu128
torch==2.8.0
torchaudio==2.8.0
